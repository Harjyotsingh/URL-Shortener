# URL Shortener

A simple Flask-based URL Shortener that converts long URLs into short links.

## Features
- Shortens URLs
- Redirects to original URL
- Simple and responsive UI

## Technologies
- Python
- Flask
- HTML
- CSS

## Run

pip install -r requirements.txt
python app.py
